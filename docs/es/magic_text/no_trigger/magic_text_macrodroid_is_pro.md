# ✨ Magic Text: `{macrodroid_is_pro}`

**Returns:** Boolean value (`true` or `false`) indicating if MacroDroid is the Pro version.

**Usage:**  
Can be used in any text field that supports Magic Text (e.g., notifications, TTS, logs).
