# ✨ Magic Text: `{logcat_line}`

**Returns:** The specific logcat entry line captured by the Logcat trigger in the macro.

**Requires:** The macro must include the **Logcat message** trigger configured to capture logs.
